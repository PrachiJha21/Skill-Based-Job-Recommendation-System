#include "user.h"

/*
  All logic for User is intentionally lightweight.
  Most functionality is implemented inline in user.h
  to keep User simple and reusable.

  This file exists for:
  - Proper compilation unit separation
  - Future extensibility
  - Clean project structure
*/

// All core User logic is inline in the header. No out-of-line definitions needed here.
